// import { Category } from "./getScore";

// export type State = {
//   turn: null | { set: number[]; k: number };
//   sheet: Sheet;
// };

// export type Sheet = Record<Category, number>;

// export type Action = { type: "reset" } | { type: "pick-dice"; dice: number };

// export const reduce = (state: State, action: Action): State => {
//   switch (action.type) {
//     case "pick-dice":
//     case "reset":
//   }
// };
